version: "3.9"
services:
  neurolinks-backoffice:
    build: .
    env_file:
      - .env
    ports:
      - "3008:8080"
    #volumes:
      #- credentialGK: /credentials
      # mongo: /data/db
      # mysql: /var/lib/mysql
      # postgres: /var/lib/postgres/data

#volumes:
  #credentialGK: 
